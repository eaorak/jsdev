import './index.css';
import chalk from 'chalk';

/* eslint-disable no-console */

const name = 'Ender';
const year = 2017;
debugger; // eslint-disable-line no-debugger
console.log(chalk.red(`Hi ${name}. This is index at ${year}!`));
