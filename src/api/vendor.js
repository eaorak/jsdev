/* This file contains references to the vendor libraries we're using in the project.
This is used by webpack only in the production build to split vendor bundle to make
caching possible on the client side for unchanged vendor libraries. */

/* eslint-disable no-unused-vars */

import fetch from "whatwg-fetch";
import jquery from "jquery";
import bootstrap from "bootstrap";
